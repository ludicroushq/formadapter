# Changesets

This folder contains changesets used to version and publish FormAdapter packages.

To release `formadapter@0.0.1` or later, add a changeset in a pull request and let the release workflow publish after it lands on `main`.

