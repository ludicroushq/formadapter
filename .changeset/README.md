# Changesets

This folder contains changesets used to version and publish FormAdapter packages.

To release one or more `@formadapter/*` packages, add a changeset in a pull
request and let the release workflow publish after it lands on `main`.

All ten public packages are one fixed group, so every release publishes them at
the same version. Private documentation and example workspaces are not
versioned.
