# FormAdapter

Automatic schema forms for React and your UI kit.

This package is in early development and is not ready for production use yet.

```ts
import { hello } from "@formadapter/react";

console.log(hello);
```
